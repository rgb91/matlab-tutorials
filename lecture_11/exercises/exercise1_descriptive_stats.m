%% EXERCISE 1: Descriptive Statistics
%  Session 11 — In-class exercise (~10 min)
%  ─────────────────────────────────────────────────────
%  Data: exercise_data.mat contains alpha peak amplitude (uV) for the
%  same subjects recorded with eyes_open and eyes_closed.
%
%  TASKS:
%  1. Load the data and report n for each condition
%  2. Compute mean, median, std, and SEM for BOTH conditions
%  3. Print a tidy summary table
%  4. Plot the two group means as a bar chart with SEM error bars

%% TODO 1: Load exercise_data.mat and print n for each condition
% Variables: eyes_open, eyes_closed, measure



%% TODO 2 & 3: Compute mean/median/std/SEM for each, print a summary table
% Hint: SEM = std(x) / sqrt(numel(x))
% Loop over the two conditions to avoid repeating code.



%% TODO 4: Bar chart of the two means with SEM error bars
% Use bar(...) then errorbar(..., 'LineStyle','none')
% Label the x-axis ticks 'eyes open' and 'eyes closed'.


